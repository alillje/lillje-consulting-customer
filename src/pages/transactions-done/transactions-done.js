import Transactions from '../transactions/transactions'

const TransactionsDone = () => {

            return (
                <Transactions value="done" />
  );
};

export default TransactionsDone;
